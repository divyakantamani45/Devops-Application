spring:
  config:
    import: aws-secretsmanager:arn:aws:secretsmanager:us-east-1:904331955008:secret:devops-secretmanager-BvG8SM

cloud:
  aws:
    region:
      static: us-east-1                 # 👈 keep keys as-is (DB_USERNAME, DB_PASSWORD, DB_NAME)

logging:
  level:
    io.awspring.cloud.secretsmanager: DEBUG
    org.springframework.boot.context.config: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: env,health
