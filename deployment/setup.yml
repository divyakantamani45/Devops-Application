apiVersion: v1
kind: ServiceAccount
metadata:
  name: devops-sa
  namespace: devops-app
  annotations:
    # replace below IAM role ARN with the role you created for IRSA
    eks.amazonaws.com/role-arn: arn:aws:iam::904331955008:role/devops-eks-prod-pod-role

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-app-ingress
  namespace: devops-app
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    # Uncomment and replace with your ACM certificate ARN if you want HTTPS
    # alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:us-east-1:123456789012:certificate/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
    # Optional: redirect HTTP->HTTPS
    alb.ingress.kubernetes.io/ssl-redirect: "443"
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-app
                port:
                  number: 80
